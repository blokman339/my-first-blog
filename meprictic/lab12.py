from threading import Event

from anyio.abc import value


#class Cat:

    #def __init__(self, name):
        #self.name = name
        #self.value = "Мяу"

#class Dog:

    #def __init__(self, name):
       # self.name = name
       # self.value = "Гав"

#cat = Cat("Шаурма")
#dog = Dog("Бобик")

#print(f"{dog.name} говорит", dog.value)
#print(f"{cat.name} говорит", cat.value)
#import pygame

#pygame.init()
#dis = pygame.display.set_mode((500,400))
#dis_over = False
#while not dis_over:
   # for event in pygame.event.get():
     #   if event.type == pygame.QUIT:
       #  print(event)
       # pygame.quit()
   # house = pygame.Rect(150, 175, 200, 200)
   # color = (155, 144, 20)
   # pygame.draw.rect(dis, color, house, 0)
   # door = pygame.Rect(225, 275, 50, 100)
   # color = (155, 0, 0)
    #pygame.draw.rect(dis, color, door, 0)
    #window = pygame.Rect(275, 200, 50, 50)
    #color = (100, 0, 0)
    #pygame.draw.rect(dis, color, window, 0)
    #color = (155, 14, 76)
    #pygame.draw.line(dis, color,    [150, 175],       [250, 75], width=6)
    #pygame.draw.line(dis, color,    [250, 75],       [350, 175], width=6)
    #pygame.display.update()
#quit()